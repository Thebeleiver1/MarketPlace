<div id = "header">
    <h2>  </h2>

    <% if user_signed_in? %>
        <h2><%= current_user.id%> </h2>
        
    <% else %>
        <p> Hello############################### sign up</p>
    <% end%>
</div>