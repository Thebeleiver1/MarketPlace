<p id="notice"><%= notice %></p>
<% if false%>
  <% @products.each do |product| %>
      <tr>
      <small> Posted by <%= product.user.first_name%>
        <td><%= product.name %></td>
        <td><%= product.description %></td>
        <td><%= product.price %></td>
        <td><%= product.condition %></td>
        <td><%= product.location %></td>
        
          <%= image_tag(product.images[0].url,style: 'height:10%;width:10%;' )%>
        
        <td><%= product.category %></td>
        <td><%= product.sellerId %></td>
        <td><%= link_to 'Show', product %></td>
        <td><%= link_to 'Edit', edit_product_path(product) %></td>
        <td><%= link_to 'Destroy', product, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
<% end %>


<br>

<div class="container">
  <h2>Products</h2>
  <div class="table-responsive">          
  
    
    
    <% @products.each do |product| %>
    <table class="table">
      <thead> 
        
        <tr>
          <td><%= image_tag(product.images[0].url,style: 'height:100px;width:150px;' )%></td>
        </tr>
        <tr>
          <td><%= product.name %></td>
          <td><%= product.condition %></td>
        </tr>
        <tr>
          <td><%= product.price %></td>
          
        </tr>
        <tr>
          <td><button type="button" class="btn btn-success">Buy Now</button></td>
        </tr>
        <tr>
          <td><button type="button" class="btn btn-info">More Info</button></td>
        </tr>
      </thead>
    </table>
    <% end %>
    

    
  
  </div>
</div>



